spring:
  main:
    web-application-type: none
  datasource:
    url: jdbc:h2:mem:vaaskel_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  liquibase:
    enabled: false
vaadin:
  launch-browser: false
